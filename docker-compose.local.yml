services:
  postgres:
    image: postgres:16
    container_name: ingles-postgres-local
    environment:
      POSTGRES_DB: english_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d english_db"]
      interval: 3s
      timeout: 3s
      retries: 10

  backend:
    build:
      context: ./backend
    container_name: ingles-backend-local
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/english_db
      FLASK_SECRET_KEY: dev-local-secret-key
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: Admin123*
      STUDENT1_USERNAME: estudiante1
      STUDENT1_PASSWORD: Estudiante123*
      STUDENT2_USERNAME: estudiante2
      STUDENT2_PASSWORD: Estudiante123*
    depends_on:
      postgres:
        condition: service_healthy
